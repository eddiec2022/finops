# Frontend dashboard (React/TypeScript/Vite/Tailwind)

See repo root `README.md` for how this fits into the Docker Compose stack.

## Local commands (inside the `frontend` container or with Node installed locally)

```
npm install
npm run dev       # Vite dev server on :5173
npm run build     # type-check + production build
```
