TASK 21: TWO INDEPENDENT CLEANUP FIXES
=======================================
Status: COMPLETE. Both items implemented, tested, and live-verified against
the running dev stack. Zero regressions in either the full backend or full
frontend test suite.

SUMMARY
-------
Two genuinely independent fixes, as scoped:

1. frontend/src/components/SpendPanel.tsx's trailing-spend sum now uses an
   actual calendar-date cutoff instead of an array slice, so it no longer
   silently over-counts (and mislabels) when a resource's cost history has a
   sync gap. Verified against real dev data: for the "nise-dev" VM, the old
   logic reported "last 30 days: $254.06" while actually summing data
   spanning 54 real calendar days; the new logic correctly reports
   "last 30 days: $17.19" from the 7 actual entries that fall inside a true
   30-calendar-day window. See "ITEM 1" below for the full walkthrough.

2. backend/app/api/recommendations.py's /reserved-instances endpoint no
   longer silently discards AWS's field_mapping_note when both Azure and AWS
   have recommendations - field_mapping_note becomes {"azure": ..., "aws": ...}
   when both providers are configured, and stays the pre-existing plain
   string when only one is (which is still every real case in this dev
   environment today, since no AWS account is connected). See "ITEM 2" below.

No AWS live-account work was touched (still blocked - see docs/governance's
renumbering note that this is now called "Task 22"). No other SpendPanel or
dashboard changes were made beyond the one described here.


ITEM 1: SpendPanel's trailing-spend sum (calendar-date cutoff, not array slice)
================================================================================

THE BUG
-------
backend/app/services/forecast.py builds `history.series` with one row per
date that has an actual cost_record - a sync gap (a day nothing was synced
for) or a genuinely uncosted day is simply *absent* from the array, not
present with a $0 entry. The old frontend code:

    function sumTrailingActual(history: CostHistoryResponse, days: number): number {
      return history.series.slice(-days).reduce((total, point) => total + point.actual_cost, 0);
    }

took the last N *array entries*, not the last N *calendar days*. Whenever a
gap exists anywhere in the window, slice(-days) silently reaches further
back in real time than N days to find N entries - and the UI's own label
("Spend, last N days") was computed the same buggy way
(Math.min(TRAILING_SPEND_DAYS, series.length)), so the label and the number
drifted together and gave no visible indication anything was wrong.

THE FIX
-------
New file frontend/src/lib/sumTrailingActual.ts: computes a real date cutoff
anchored on the series' own latest date (not the browser's "now" - synced
data can lag real-world today by however long since the last sync, and
anchoring on real "now" would make a resource's own freshly-synced days look
like a gap rather than "not yet synced"). Returns {windowDays, total}
together from one function, so the label and the number can never drift out
of sync again - SpendPanel.tsx now renders both directly from the same
result instead of computing them separately in two places.

This mirrors LinearBurnRateModel's identical existing principle on the
backend side (forecasting/linear_burn_rate.py: window by calendar days
ending on the most recent date with data, not by slicing the last N
entries) - same fix, now applied consistently on both sides of that exact
split between the two systems.

REAL EXAMPLE: OLD vs NEW BEHAVIOR DIFFER FOR A RESOURCE WITH A REAL SYNC GAP
-----------------------------------------------------------------------------
Ran directly against the live dev database (not a constructed fixture) -
resource "nise-dev" (Azure VM, resource_id 784c331d-5586-4cc8-9c97-54f89b44b140),
via GET /api/v1/cost?resource_id=784c331d-5586-4cc8-9c97-54f89b44b140:

  total points in series: 59
  first date:  2026-06-27
  last date:   2026-09-21
  calendar span: 87 days ; actual entries: 59  ->  28 missing (gapped) days

  OLD (array slice(-30)):
    label:   "Spend, last 30 days"
    sum:     $254.06
    entries: 30
    -> the 30 array entries it grabbed actually span from 2026-07-30 to
       2026-09-21 - that's 54 real calendar days, not 30. The label lied by
       nearly 2x about the window it was summing.

  NEW (calendar-date cutoff, 30 days):
    label:   "Spend, last 30 days"
    sum:     $17.19
    entries: 7
    cutoff date: 2026-08-23
    -> only the 7 entries that actually fall within the true last 30
       calendar days (2026-08-23 through 2026-09-21) are summed. Label and
       number are now both honest about the same window.

This is a ~14.8x difference in the reported dollar figure for a real
resource, produced by a real historical billing/sync gap already present in
this dev environment's data - not a hypothetical.

UNIT TESTS (frontend/src/lib/sumTrailingActual.test.ts, 5 tests)
------------------------------------------------------------------
  1. Empty series -> {windowDays: 0, total: 0}.
  2. Dense/gap-free 40-day series -> matches old slice-based behavior exactly
     for the no-gap case ($30 over the trailing 30 days), confirming this is
     a targeted fix, not a behavior change for the common case.
  3. Shorter history than the requested window (10 days of a 30-day
     request) -> caps windowDays to the real 10-day span, sum $20.
  4. The exact gap scenario from the task description (35-day span, 25
     entries due to a 10-day gap in the middle): asserts new sum $60 vs the
     old buggy sum $75, and new windowDays=30 vs the old label of 25 -
     explicitly asserts the new result is LESS than the old buggy sum, since
     the bug always over-counts (never under-counts).
  5. Anchoring check: a series that stops well before the real "now" the
     test runs under must still report a full 30-day window ending on its
     own latest date, not look like it has a gap right before real-world
     today.


ITEM 2: recommendations.py's field_mapping_note (merge Azure + AWS notes)
============================================================================

THE BUG
-------
get_reservation_recommendations() in backend/app/api/recommendations.py
called Azure's build_reservation_recommendations() (which sets its own
field_mapping_note), then, when AWS is configured, appended AWS's
recommendations to the list - but never touched field_mapping_note itself.
Azure's note silently stood in for both providers once AWS's items were
merged into the same response; AWS's own field-mapping caveat was discarded
entirely, even though AWS recommendation items were now present in the
response body carrying it.

THE FIX
-------
    if settings.aws_configured:
        aws_result = build_aws_reservation_recommendations()
        result["recommendations"] = result["recommendations"] + aws_result["recommendations"]
        result["field_mapping_note"] = {
            "azure": result["field_mapping_note"],
            "aws": aws_result["field_mapping_note"],
        }

field_mapping_note becomes {"azure": "...", "aws": "..."} only when AWS is
configured; stays the pre-existing plain string otherwise - a non-breaking
change for every current Azure-only caller (which, in this dev environment
today, is every caller - AWS is not yet connected to a live account).

Trigger condition chosen: settings.aws_configured (same condition every
other dispatch branch in this file already uses), not "did this particular
response happen to contain any AWS items." Reasoning, flagged explicitly:
the field_mapping_note caveat is about mapping trustworthiness in general
(AWS's field names not yet verified against real populated data), not about
this one response's contents - so it should appear whenever AWS is a live
contributor to this endpoint, even in the edge case where AWS's recommendation
engine happens to return zero items this particular call. Covered by a
dedicated test (test_field_mapping_note_still_a_dict_even_when_aws_has_no_items).

Frontend followed the same shape change through:
  - api/types.ts: field_mapping_note: string | { azure: string; aws: string }
  - lib/normalizeRecommendations.ts: new impactNoteFor() helper picks the
    right per-item note (r.provider === "aws" ? note.aws : note.azure) when
    the merged dict shape is present, or the whole string when it isn't -
    each item's impactNote now always reflects its own provider's caveat,
    not just whichever provider happened to be Azure/first.

BACKEND TESTS (backend/tests/test_recommendations_api.py, 3 tests - NEW FILE)
---------------------------------------------------------------------------
No endpoint-level test existed for this endpoint before (the pre-existing
reserved-instances tests only exercise the service functions directly, not
the endpoint's own dispatch/merge logic). Built using module-attribute
monkeypatching of app.api.recommendations' own imported function references
(the endpoint takes no db/fetch dependency to inject through - it's a live
pass-through by design, per reserved_instances.py's own docstring).

  1. test_field_mapping_note_stays_a_plain_string_when_aws_unconfigured
     - AWS settings blanked out; asserts the response's field_mapping_note
       is unchanged - a plain string, untouched by the merge branch, exactly
       what build_reservation_recommendations() returned.
  2. test_field_mapping_note_becomes_a_dict_with_both_providers_when_both_configured
     - both providers configured with fake results; asserts field_mapping_note
       == {"azure": AZURE_NOTE, "aws": AWS_NOTE} and both providers'
       recommendations appear in the merged list.
  3. test_field_mapping_note_still_a_dict_even_when_aws_has_no_items
     - AWS configured but its fake result returns an empty recommendations
       list; asserts field_mapping_note is still the merged dict shape (not
       silently reverted to a plain string) while only Azure's one item
       appears in the recommendations list.

FRONTEND TEST (frontend/src/lib/normalizeRecommendations.test.ts, 1 new test)
---------------------------------------------------------------------------
"Task 21: gives each item its own provider's note when field_mapping_note is
merged ({azure, aws})" - builds a response with the merged dict shape and one
item from each provider, asserts the Azure item's impactNote is Azure's own
text and the AWS item's impactNote is AWS's own text (not the same note
applied to both, and not a stringified object).


FULL TEST SUITES - RUN THIS SESSION, ZERO REGRESSIONS
=========================================================
Both Docker images (finops-backend, finops-frontend) were rebuilt from
current source via `docker compose build backend frontend` before every run
below (no source volume mounts - stale images would otherwise silently test
old code).

Backend (docker compose run --rm backend pytest -q):

  ........................................................................ [ 36%]
  ........................................................................ [ 72%]
  ......................................................                   [100%]
  198 passed, 18 warnings in 3.43s

  (18 warnings are pre-existing deprecation notices from starlette/botocore,
  unrelated to this task - not new.)

Frontend (docker compose run --rm frontend npm test -- --run):

   Test Files  7 passed (7)
        Tests  40 passed (40)

   ✓ src/lib/normalizeRecommendations.test.ts (9 tests)
   ✓ src/pages/Recommendations.test.tsx (3 tests)
   ✓ src/lib/sumTrailingActual.test.ts (5 tests)
   ✓ src/pages/Overview.test.tsx (8 tests)
   ✓ src/lib/groupResources.test.ts (11 tests)
   ✓ src/lib/combineSpendSeries.test.ts (3 tests)
   ✓ src/App.test.tsx (1 test)

Frontend production build (docker compose run --rm frontend npm run build,
tsc -b && vite build): clean, no type errors.

  ✓ 660 modules transformed.
  dist/index.html                   0.72 kB
  dist/assets/index-CAHsrOY7.css   10.42 kB
  dist/assets/index-BcJGhrV5.js   578.47 kB
  ✓ built in 3.28s

  (The "chunk larger than 500 kB" warning is pre-existing, unrelated to this
  task - not a new regression.)


LIVE VERIFICATION AGAINST THE RUNNING DEV STACK
==================================================
- GET /api/v1/cost?resource_id=784c331d-5586-4cc8-9c97-54f89b44b140 (nise-dev):
  confirmed the real $254.06 -> $17.19 differential above directly from the
  live API response (see ITEM 1's walkthrough - this is the actual endpoint
  SpendPanel.tsx calls, not a mocked/constructed response).
- GET /api/v1/recommendations/reserved-instances: confirmed field_mapping_note
  is still a plain string in this dev environment right now (AWS is not
  configured here), e.g.:
    "Field names are mapped per Microsoft's documented schema for this API,
     not verified against a real populated response - this subscription's
     usage his[tory...]"
  -> confirms item 2's fix is a no-op for every real request this
     environment currently serves, exactly as intended (non-breaking).


WSL2/DOCKERD RESTART MONITOR
===============================
  pid:            249 (unchanged all session)
  started:        Mon 2026-09-21 08:22:37 EDT
  timer:          active, still firing every 30s, most recent check_ok at
                  2026-09-21 21:47:27 EDT
  RESTART_DETECTED total: 22 (unchanged - no new restarts during this task)


DATA INTEGRITY CHECK
=======================
  utilization_metrics: 786 rows
  cost_records:         249 rows

These match the utilization_metrics count already observed at the *end* of
Task 20's session (786) - no further change during Task 21's work. For the
record (not new information, but restating since it was never written to a
results.txt before the previous session was interrupted mid-task): the
utilization_metrics count grew from 536 (last confirmed at the end of Task
20's live verification) to 786 - a jump of 250 rows - by the time this count
was first re-checked. I reviewed my own command history and confirmed I
never called POST /api/v1/utilization/sync during that session or this one.
I'm reporting this honestly as an unexplained-but-observed change rather
than guessing at a cause - it did not originate from any action I took. If
something else in this environment (a scheduled sync, another process, or
manual action) is writing to utilization_metrics outside of this agent's own
actions, that may be worth checking on your end.


DESIGN DECISIONS FLAGGED FOR REVIEW (not "not my call" hard stops - just
worth your awareness; I made a call on each and it's reflected above)
==========================================================================
1. Item 2's trigger condition: field_mapping_note becomes the merged dict
   shape based on settings.aws_configured, not on whether AWS actually
   returned any items this call. I judged the caveat to be about mapping
   trustworthiness in general (not response-specific), so it should appear
   whenever AWS is a live contributor to the endpoint. If you'd rather it
   only merge when AWS actually contributed >=1 recommendation, that's a
   small, easy change to test_field_mapping_note_still_a_dict_even_when_aws_has_no_items's
   expected outcome.
2. Item 1 anchors the "last N days" window on the series' own latest synced
   date, not the browser's real-world "now". This means a resource that
   hasn't synced in a while will still show a full N-day window ending on
   its last known data, not a window that looks increasingly gapped as real
   time passes without a new sync. I judged this the more honest choice
   (matches LinearBurnRateModel's existing principle), but it does mean the
   UI won't visually warn a user "this resource hasn't synced recently" via
   this component - that's a separate, out-of-scope concern if you want it
   surfaced elsewhere.

Neither of these is a "not my call" boundary under CLAUDE.md's rule - both
are implementation judgment calls within the task's stated scope, flagged
here so you can override either if you'd have picked differently.


FULL DIFF
===========
diff --git a/backend/app/api/recommendations.py b/backend/app/api/recommendations.py
index e4763ac..8d53b0d 100644
--- a/backend/app/api/recommendations.py
+++ b/backend/app/api/recommendations.py
@@ -55,5 +55,19 @@ def get_reservation_recommendations() -> dict[str, Any]:
     # dependency the way the other three recommendation endpoints do.
     result = build_reservation_recommendations()
     if settings.aws_configured:
-        result["recommendations"] = result["recommendations"] + build_aws_reservation_recommendations()["recommendations"]
+        aws_result = build_aws_reservation_recommendations()
+        result["recommendations"] = result["recommendations"] + aws_result["recommendations"]
+        # Task 21 fix: each provider's field_mapping_note is its own caveat
+        # about its own field-name mapping - Azure's alone silently stood in
+        # for both once AWS's recommendations were merged in above, discarding
+        # AWS's own note entirely. Triggered on aws_configured (same condition
+        # every other dispatch in this file already uses), not on whether this
+        # particular call happened to find any AWS items - the caveat is about
+        # mapping trustworthiness in general, not this one response. Stays a
+        # plain string (the pre-existing shape) whenever AWS isn't configured,
+        # so this is not a breaking change for every current Azure-only caller.
+        result["field_mapping_note"] = {
+            "azure": result["field_mapping_note"],
+            "aws": aws_result["field_mapping_note"],
+        }
     return result
diff --git a/frontend/src/api/types.ts b/frontend/src/api/types.ts
index 2b4a849..3d8d50c 100644
--- a/frontend/src/api/types.ts
+++ b/frontend/src/api/types.ts
@@ -148,6 +148,11 @@ export interface ReservedInstanceRecommendation {
 
 export interface ReservedInstancesResponse {
   source: string;
-  field_mapping_note: string;
+  // Task 21: a plain string covers both providers' caveats when only one
+  // contributed recommendations (e.g. AWS unconfigured) - the pre-existing
+  // shape, unchanged for that case. Becomes {azure, aws} once both providers
+  // are configured, so neither provider's own mapping caveat is silently
+  // dropped once their recommendations are merged into one list.
+  field_mapping_note: string | { azure: string; aws: string };
   recommendations: ReservedInstanceRecommendation[];
 }
diff --git a/frontend/src/components/SpendPanel.tsx b/frontend/src/components/SpendPanel.tsx
index c1934bf..7aa7130 100644
--- a/frontend/src/components/SpendPanel.tsx
+++ b/frontend/src/components/SpendPanel.tsx
@@ -3,6 +3,7 @@ import { getCostHistory, getForecast } from "../api/client";
 import type { CostHistoryResponse, ForecastResponse } from "../api/types";
 import { combineSpendSeries } from "../lib/combineSpendSeries";
 import { formatCurrency } from "../lib/formatCurrency";
+import { sumTrailingActual } from "../lib/sumTrailingActual";
 import { EmptyState } from "./EmptyState";
 import { SpendChart } from "./SpendChart";
 
@@ -15,10 +16,6 @@ type LoadState =
   | { status: "empty" }
   | { status: "ready"; history: CostHistoryResponse; forecast: ForecastResponse };
 
-function sumTrailingActual(history: CostHistoryResponse, days: number): number {
-  return history.series.slice(-days).reduce((total, point) => total + point.actual_cost, 0);
-}
-
 interface SpendPanelProps {
   /** Scopes the underlying /cost and /forecast calls to one resource group. Omit for the aggregate view. */
   resourceGroup?: string;
@@ -70,14 +67,14 @@ export function SpendPanel({ resourceGroup, resourceId, emptyStateDescription }:
     return <EmptyState description={emptyStateDescription} />;
   }
 
+  const trailing = sumTrailingActual(state.history, TRAILING_SPEND_DAYS);
+
   return (
     <div className="space-y-6">
       <div className="rounded-xl bg-white p-6 shadow-sm">
-        <p className="text-sm text-stone-500">
-          Spend, last {Math.min(TRAILING_SPEND_DAYS, state.history.series.length)} days
-        </p>
+        <p className="text-sm text-stone-500">Spend, last {trailing.windowDays} days</p>
         <p className="mt-1 font-mono text-4xl font-semibold tabular-nums text-brand-dark">
-          {formatCurrency(sumTrailingActual(state.history, TRAILING_SPEND_DAYS))}
+          {formatCurrency(trailing.total)}
         </p>
       </div>
 
diff --git a/frontend/src/lib/normalizeRecommendations.test.ts b/frontend/src/lib/normalizeRecommendations.test.ts
index 3bdd228..eab911f 100644
--- a/frontend/src/lib/normalizeRecommendations.test.ts
+++ b/frontend/src/lib/normalizeRecommendations.test.ts
@@ -226,4 +226,18 @@ describe("normalizeReservedInstances", () => {
     expect(item.reason).toBe("$0.05/hr commitment, ONE_YEAR term");
     expect(item.estimatedMonthlyImpact).toBe(18);
   });
+
+  it("Task 21: gives each item its own provider's note when field_mapping_note is merged ({azure, aws})", () => {
+    const items = normalizeReservedInstances({
+      source: "merged",
+      field_mapping_note: { azure: "Azure caveat text.", aws: "AWS caveat text." },
+      recommendations: [
+        { provider: "azure", id: "azure-1", sku: "Standard_D2s_v3", location: "eastus" },
+        { provider: "aws", recommendation_type: "reserved_instance", instance_type: "m5.large", location: "us-east-1" },
+      ],
+    });
+
+    expect(items[0].impactNote).toBe("Azure caveat text.");
+    expect(items[1].impactNote).toBe("AWS caveat text.");
+  });
 });
diff --git a/frontend/src/lib/normalizeRecommendations.ts b/frontend/src/lib/normalizeRecommendations.ts
index 156ab25..cc36e7f 100644
--- a/frontend/src/lib/normalizeRecommendations.ts
+++ b/frontend/src/lib/normalizeRecommendations.ts
@@ -135,12 +135,19 @@ function normalizeReservedInstanceItem(r: ReservedInstanceRecommendation, index:
   };
 }
 
+// field_mapping_note is a plain string when only one provider contributed
+// recommendations (the pre-existing shape), or {azure, aws} once both did
+// (Task 21) - each item gets its own provider's note in the merged case,
+// rather than one note (or a stringified object) applied to every item
+// regardless of which provider it actually came from.
+function impactNoteFor(r: ReservedInstanceRecommendation, note: ReservedInstancesResponse["field_mapping_note"]): string {
+  if (typeof note === "string") return note;
+  return r.provider === "aws" ? note.aws : note.azure;
+}
+
 export function normalizeReservedInstances(response: ReservedInstancesResponse): RecommendationItem[] {
   return response.recommendations.map((r, index) => ({
     ...normalizeReservedInstanceItem(r, index),
-    // field_mapping_note is response-level (shared across every item, from
-    // whichever provider's build_*_reservation_recommendations produced it) -
-    // applied here rather than per-item, same as the original single-provider version.
-    impactNote: response.field_mapping_note,
+    impactNote: impactNoteFor(r, response.field_mapping_note),
   }));
 }
=== NEW FILE: backend/tests/test_recommendations_api.py ===
"""Endpoint-level dispatch tests for /api/v1/recommendations/* - specifically
the reserved-instances merge logic (Task 21 item 2). No existing test exercised
this endpoint's dispatch before (the pre-existing reserved-instances tests only
call the service functions directly), so both the "AWS unconfigured" case and
the new "both providers merged" case are new here.

The reserved-instances endpoint is a live pass-through to each provider's own
recommendation engine (see reserved_instances.py's own docstring) - there's no
DB-seeded data to drive it the way the other three categories' endpoint tests
work. Monkeypatching the module-level function references the endpoint calls
(as imported into app.api.recommendations' own namespace) is the standard,
precise way to test its dispatch logic without live Azure/AWS credentials."""

import app.api.recommendations as recommendations_api
from app.core.config import settings

AZURE_NOTE = "Azure field names unverified against real populated data."
AWS_NOTE = "AWS field names unverified against real populated data."


def _fake_azure_result(items=None):
    # `items is None` (not falsy) distinguishes "use the default" from a
    # deliberately empty list - `[] or default` would silently fall back to
    # the default too, since an empty list is falsy in Python.
    return {
        "provider": "azure",
        "source": "Azure Microsoft.Consumption reservationRecommendations API",
        "field_mapping_note": AZURE_NOTE,
        "recommendations": items if items is not None else [{"id": "azure-1", "sku": "Standard_D2s_v3"}],
    }


def _fake_aws_result(items=None):
    return {
        "provider": "aws",
        "source": "AWS Cost Explorer APIs",
        "field_mapping_note": AWS_NOTE,
        "recommendations": (
            items if items is not None else [{"recommendation_type": "reserved_instance", "instance_type": "m5.large"}]
        ),
    }


def test_field_mapping_note_stays_a_plain_string_when_aws_unconfigured(client, monkeypatch):
    monkeypatch.setattr(settings, "aws_account_id", "")
    monkeypatch.setattr(settings, "aws_region", "")
    monkeypatch.setattr(settings, "aws_access_key_id", "")
    monkeypatch.setattr(settings, "aws_secret_access_key", "")
    monkeypatch.setattr(recommendations_api, "build_reservation_recommendations", _fake_azure_result)

    response = client.get("/api/v1/recommendations/reserved-instances")

    assert response.status_code == 200
    body = response.json()
    # Unchanged shape - a plain string, exactly what build_reservation_recommendations
    # returned, not touched by the merge logic at all since AWS never runs.
    assert body["field_mapping_note"] == AZURE_NOTE
    assert isinstance(body["field_mapping_note"], str)
    assert len(body["recommendations"]) == 1


def test_field_mapping_note_becomes_a_dict_with_both_providers_when_both_configured(client, monkeypatch):
    monkeypatch.setattr(settings, "aws_account_id", "999988887777")
    monkeypatch.setattr(settings, "aws_region", "us-east-1")
    monkeypatch.setattr(settings, "aws_access_key_id", "testing")
    monkeypatch.setattr(settings, "aws_secret_access_key", "testing")
    monkeypatch.setattr(recommendations_api, "build_reservation_recommendations", _fake_azure_result)
    monkeypatch.setattr(recommendations_api, "build_aws_reservation_recommendations", _fake_aws_result)

    response = client.get("/api/v1/recommendations/reserved-instances")

    assert response.status_code == 200
    body = response.json()
    # Task 21 fix: both providers' own notes are now present and distinguishable,
    # not just Azure's silently standing in for both.
    assert body["field_mapping_note"] == {"azure": AZURE_NOTE, "aws": AWS_NOTE}
    assert len(body["recommendations"]) == 2
    ids_or_types = {r.get("id") or r.get("recommendation_type") for r in body["recommendations"]}
    assert ids_or_types == {"azure-1", "reserved_instance"}


def test_field_mapping_note_still_a_dict_even_when_aws_has_no_items(client, monkeypatch):
    # Per this task's chosen trigger: keyed on aws_configured, not on whether
    # this particular call happened to find any AWS items - flagged reasoning
    # in results.txt. AWS's own note is still meaningful even with zero items
    # this time (it's a caveat about mapping trustworthiness in general).
    monkeypatch.setattr(settings, "aws_account_id", "999988887777")
    monkeypatch.setattr(settings, "aws_region", "us-east-1")
    monkeypatch.setattr(settings, "aws_access_key_id", "testing")
    monkeypatch.setattr(settings, "aws_secret_access_key", "testing")
    monkeypatch.setattr(recommendations_api, "build_reservation_recommendations", _fake_azure_result)
    monkeypatch.setattr(recommendations_api, "build_aws_reservation_recommendations", lambda: _fake_aws_result(items=[]))

    response = client.get("/api/v1/recommendations/reserved-instances")

    body = response.json()
    assert body["field_mapping_note"] == {"azure": AZURE_NOTE, "aws": AWS_NOTE}
    assert len(body["recommendations"]) == 1  # only Azure's one item

=== NEW FILE: frontend/src/lib/sumTrailingActual.ts ===
import type { CostHistoryResponse } from "../api/types";

export interface TrailingSpend {
  /** Actual calendar days the window covers - min(days, the series' own span) - always matches `total`'s window. */
  windowDays: number;
  total: number;
}

export const EMPTY_TRAILING_SPEND: TrailingSpend = { windowDays: 0, total: 0 };

/**
 * Sums actual_cost within the trailing `days` calendar days, anchored on the
 * *data's own latest date*, not "today" - not an array slice.
 *
 * Task 21 fix: `history.series` only has a row for a date with an actual
 * cost_record (backend/app/services/forecast.py's GROUP BY) - a sync gap or a
 * genuinely zero-cost day is simply absent, not present with a zero value. The
 * old `series.slice(-days)` took the last N *array entries*, which silently
 * reaches further back in time than N calendar days whenever a gap exists
 * anywhere in the window - the UI's own "last 30 days" label became
 * inaccurate with no visible indication. Filtering by an actual date cutoff
 * fixes that.
 *
 * Anchored on the series' latest date rather than the browser's current date,
 * deliberately: synced data can lag real "today" by however long it's been
 * since the last sync, and anchoring on real "today" would make a resource's
 * own most-recently-synced days look like a gap rather than "not synced yet".
 * This mirrors LinearBurnRateModel's identical choice server-side
 * (forecasting/linear_burn_rate.py: "Window by calendar days ending on the
 * most recent date with data, not... by slicing the last N entries") - same
 * principle, now applied consistently on both sides of this exact split.
 */
export function sumTrailingActual(history: CostHistoryResponse, days: number): TrailingSpend {
  if (history.series.length === 0) return EMPTY_TRAILING_SPEND;

  const earliest = history.series[0].date;
  const latest = history.series[history.series.length - 1].date;
  const spanDays = Math.round((Date.parse(latest) - Date.parse(earliest)) / 86_400_000) + 1;
  const windowDays = Math.min(days, spanDays);

  const cutoff = new Date(latest);
  cutoff.setUTCDate(cutoff.getUTCDate() - (windowDays - 1));
  const cutoffDate = cutoff.toISOString().slice(0, 10);

  const total = history.series
    .filter((point) => point.date >= cutoffDate)
    .reduce((sum, point) => sum + point.actual_cost, 0);

  return { windowDays, total };
}

=== NEW FILE: frontend/src/lib/sumTrailingActual.test.ts ===
import { describe, expect, it } from "vitest";
import type { CostHistoryPoint } from "../api/types";
import { sumTrailingActual } from "./sumTrailingActual";

function point(date: string, actual_cost: number): CostHistoryPoint {
  return { date, actual_cost };
}

/** N consecutive calendar days starting at `startDate`, one point per day - the dense/no-gap case. */
function denseDays(startDate: string, count: number, costPerDay: number): CostHistoryPoint[] {
  const start = new Date(startDate);
  return Array.from({ length: count }, (_, i) => {
    const d = new Date(start);
    d.setUTCDate(d.getUTCDate() + i);
    return point(d.toISOString().slice(0, 10), costPerDay);
  });
}

describe("sumTrailingActual", () => {
  it("returns zero for an empty series", () => {
    expect(sumTrailingActual({ series: [] }, 30)).toEqual({ windowDays: 0, total: 0 });
  });

  it("sums a dense (gap-free) series exactly like the old slice-based logic did", () => {
    // 40 consecutive days, $1/day - last 30 calendar days should be $30, matching
    // the old series.slice(-30) result for this same gap-free case exactly.
    const series = denseDays("2026-08-01", 40, 1);

    const result = sumTrailingActual({ series }, 30);

    expect(result.windowDays).toBe(30);
    expect(result.total).toBe(30);
  });

  it("caps the window to the series' own span when there's less history than requested", () => {
    const series = denseDays("2026-09-01", 10, 2);

    const result = sumTrailingActual({ series }, 30);

    expect(result.windowDays).toBe(10);
    expect(result.total).toBe(20);
  });

  it("Task 21 regression: a gap in the trailing window no longer silently reaches further back than the claimed calendar-day window", () => {
    // The exact scenario the task describes: 35 calendar days of real coverage,
    // but only 25 actual entries (10 zero-cost days in the middle were never
    // written as cost_record rows, so they're simply absent from the series,
    // not present with a zero value).
    const allDays = denseDays("2026-08-01", 35, 3); // $3/day for every real day in the 35-day span
    // Drop days 10-19 (0-indexed) - a 10-day gap in the middle of the window.
    const withGap = [...allDays.slice(0, 10), ...allDays.slice(20)];
    expect(withGap.length).toBe(25); // sanity-check the fixture itself

    const result = sumTrailingActual({ series: withGap }, 30);

    // OLD (buggy) behavior: series.slice(-30) on a 25-entry array returns all
    // 25 entries (since 25 < 30) - claims "last 25 days" via series.length,
    // but actually sums cost spanning the full 35-calendar-day range, quietly
    // reaching 5 days further back than either the label or the 30-day cap
    // claims.
    const oldBuggyLabel = Math.min(30, withGap.length); // 25 - what the old label showed
    const oldBuggySum = withGap.slice(-30).reduce((t, p) => t + p.actual_cost, 0); // sums all 25 entries = $75

    // NEW (fixed) behavior: windowDays correctly reflects the real 30-calendar-day
    // cap (not capped further by entry count), and the sum only includes entries
    // that actually fall within the last 30 calendar days of the 35-day span -
    // excluding the first 5 days of real data, which fall outside that window.
    expect(result.windowDays).toBe(30);
    expect(result.windowDays).not.toBe(oldBuggyLabel);

    // Last 30 calendar days of a 35-day span excludes the first 5 days. Days
    // 0-4 (5 real entries, $3 each = $15) are outside the window; the gap
    // itself (days 10-19) contributes nothing either way since those entries
    // never existed. Remaining real entries inside the window: 25 - 5 = 20,
    // at $3 each = $60.
    expect(result.total).toBe(60);
    expect(result.total).not.toBe(oldBuggySum);
    expect(result.total).toBeLessThan(oldBuggySum);
  });

  it("anchors on the series' own latest date, not the calling environment's current date", () => {
    // Data that stops well before "now" (whatever "now" is when this test
    // runs) must still report a full 30-day window ending on its own last
    // synced date, not look like it has a 30-day gap right before "today".
    const series = denseDays("2020-01-01", 30, 5);

    const result = sumTrailingActual({ series }, 30);

    expect(result.windowDays).toBe(30);
    expect(result.total).toBe(150);
  });
});

